language: cpp

os:
  - linux

install:
  - sudo apt-get update -qq
  - sudo apt-get install -y fpc

script:
  - cd Build
  - ENGINE_PATH="../Engine"
  - chmod +x compile_tests.sh
  - ./compile_tests.sh
  - ./TERRATest
