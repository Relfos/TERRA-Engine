language: cpp

os:
  - linux

install:
  - ENGINE_PATH=Engine
  - TESTS_PATH=Tests
  - sudo apt-get update -qq
  - sudo apt-get install -y fpc

script:
  - chmod +x Build/compile_tests.sh
  - ./Build/compile_tests.sh
  - ./TERRATest
